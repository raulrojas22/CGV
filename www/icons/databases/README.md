# External database icons

Drop your `.ico` files in this folder using these exact names:

- `mygene.ico`
- `ncbi.ico`
- `uniprot.ico`
- `ensembl.ico`

If any icon is missing, the UI falls back to `www/icons/DNA.ico`.
